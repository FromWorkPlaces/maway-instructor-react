import React from 'react'
import { Link } from 'react-router-dom'

const Schedule = () => {
    return (
        <div>
            <button><Link to='/add_schedule'>Add Schedule</Link></button>
        </div>
    )
}

export default Schedule
